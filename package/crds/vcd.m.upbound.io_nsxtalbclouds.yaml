---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: nsxtalbclouds.vcd.m.upbound.io
spec:
  group: vcd.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - vcd
    kind: NsxtAlbCloud
    listKind: NsxtAlbCloudList
    plural: nsxtalbclouds
    singular: nsxtalbcloud
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NsxtAlbCloud is the Schema for the NsxtAlbClouds API. Provides
          a resource to manage ALB Clouds for Providers. An NSX-T Cloud is a service
          provider-level construct that consists of an NSX-T Manager and an NSX-T
          Data Center transport zone.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NsxtAlbCloudSpec defines the desired state of NsxtAlbCloud
            properties:
              forProvider:
                properties:
                  controllerId:
                    description: |-
                      ALB Controller ID
                      NSX-T ALB Controller ID
                    type: string
                  description:
                    description: |-
                      An optional description ALB Cloud
                      NSX-T ALB Cloud description
                    type: string
                  importableCloudId:
                    description: |-
                      Importable Cloud ID. Can be looked up using vcd_nsxt_alb_importable_cloud data
                      source
                      NSX-T ALB Importable Cloud ID
                    type: string
                  name:
                    description: |-
                      A name for ALB Cloud
                      NSX-T ALB Cloud name
                    type: string
                  networkPoolId:
                    description: |-
                      Network pool ID for ALB Cloud. Can be looked up using vcd_nsxt_alb_importable_cloud data
                      source
                      Network pool ID for NSX-T ALB Importable Cloud
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  controllerId:
                    description: |-
                      ALB Controller ID
                      NSX-T ALB Controller ID
                    type: string
                  description:
                    description: |-
                      An optional description ALB Cloud
                      NSX-T ALB Cloud description
                    type: string
                  importableCloudId:
                    description: |-
                      Importable Cloud ID. Can be looked up using vcd_nsxt_alb_importable_cloud data
                      source
                      NSX-T ALB Importable Cloud ID
                    type: string
                  name:
                    description: |-
                      A name for ALB Cloud
                      NSX-T ALB Cloud name
                    type: string
                  networkPoolId:
                    description: |-
                      Network pool ID for ALB Cloud. Can be looked up using vcd_nsxt_alb_importable_cloud data
                      source
                      Network pool ID for NSX-T ALB Importable Cloud
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.controllerId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.controllerId)
                || (has(self.initProvider) && has(self.initProvider.controllerId))'
            - message: spec.forProvider.importableCloudId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.importableCloudId)
                || (has(self.initProvider) && has(self.initProvider.importableCloudId))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.networkPoolId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.networkPoolId)
                || (has(self.initProvider) && has(self.initProvider.networkPoolId))'
          status:
            description: NsxtAlbCloudStatus defines the observed state of NsxtAlbCloud.
            properties:
              atProvider:
                properties:
                  controllerId:
                    description: |-
                      ALB Controller ID
                      NSX-T ALB Controller ID
                    type: string
                  description:
                    description: |-
                      An optional description ALB Cloud
                      NSX-T ALB Cloud description
                    type: string
                  healthMessage:
                    description: |-
                      DetailedHealthMessage contains detailed message on the health of the Cloud
                      NSX-T ALB Cloud detailed health message
                    type: string
                  healthStatus:
                    description: |-
                      HealthStatus contains status of the Load Balancer Cloud. Possible values are:
                      NSX-T ALB Cloud health status
                    type: string
                  id:
                    type: string
                  importableCloudId:
                    description: |-
                      Importable Cloud ID. Can be looked up using vcd_nsxt_alb_importable_cloud data
                      source
                      NSX-T ALB Importable Cloud ID
                    type: string
                  name:
                    description: |-
                      A name for ALB Cloud
                      NSX-T ALB Cloud name
                    type: string
                  networkPoolId:
                    description: |-
                      Network pool ID for ALB Cloud. Can be looked up using vcd_nsxt_alb_importable_cloud data
                      source
                      Network pool ID for NSX-T ALB Importable Cloud
                    type: string
                  networkPoolName:
                    description: |-
                      Network Pool Name used by the Cloud
                      Network pool name of NSX-T ALB Cloud
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
